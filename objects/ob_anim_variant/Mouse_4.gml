/// @desc

// Inherit the parent event
event_inherited();
current_action = ds_stack_pop(map_stack)

current_action[? parameter_name] = value

ds_stack_push(map_stack, current_action)

sc_goto_previous_room()
